import { EntradaSelectItem } from './entrada-select-item';

describe('EntradaSelectItem', () => {
  it('should create an instance', () => {
    expect(new EntradaSelectItem()).toBeTruthy();
  });
});
